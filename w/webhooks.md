# Webhooks

A `webhook` is an automatic notification (ie. push) from one system to another system whenever some event happens.

A webhook is usually implemented as an HTTP POST request sent to a URL provided by the target system, with a JSON payload.

----

Back up to: [Top](../index.md)
